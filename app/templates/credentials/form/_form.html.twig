{# 
    Required parameters:
        - form : the FormView to display

    Optional parameters:
        - readonly : specifies if form fields should be ReadOnly
#}

{{ form_start(form) }}
    {{ include('main/form/_widget.html.twig', { 'formField': form.name, 'enableCopy': true }) }}
    {{ include('main/form/_widget.html.twig', { 'formField': form.username, 'enableCopy': true }) }}
    {{ include('main/form/_widget.html.twig', { 'formField': form.password, 'enableCopy': true }) }}
    {{ include('main/form/_widget.html.twig', { 'formField': form.link, 'enableCopy': true }) }}

    {% if form.register is defined %}
        {{ form_widget(form.register, { 'attr': {'class': 'btn' }}) }}
    {% endif %}
{{ form_end(form) }}
