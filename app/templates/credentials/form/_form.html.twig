{# 
    Required parameters:
        - form : the FormView to display

    Optional parameters:
        - readonly : specifies if form fields should be ReadOnly
#}

{# Tests if readonly is set and if it is boolean, sets it as false by default #}
{#{% if readonly is not defined or (readonly is not same as true and readonly is not same as false) %}
    {% set readonly = false %}
{% endif %}#}

{{ form_start(form) }}
    {{ include('credentials/form/_widget.html.twig', { 'formField': form.name}) }}
    {{ include('credentials/form/_widget.html.twig', { 'formField': form.username}) }}
    {{ include('credentials/form/_widget.html.twig', { 'formField': form.password}) }}
    {{ include('credentials/form/_widget.html.twig', { 'formField': form.link}) }}

    {% if form.register is defined %}
        {{ form_widget(form.register, { 'attr': {'class': 'btn' }}) }}
    {% endif %}
{{ form_end(form) }}
